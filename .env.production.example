# ApprovalFlow Production Environment Variables
# Add these to Vercel Dashboard → Project Settings → Environment Variables

# ============================================================================
# REQUIRED VARIABLES (Backend will fail without these)
# ============================================================================

# Database Connection (from Vercel Neon Postgres integration)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=

# JWT Authentication Secrets
# Generate with: openssl rand -base64 32
# IMPORTANT: Use different secrets in production!
JWT_SECRET=
JWT_REFRESH_SECRET=

# SendGrid Email Service
# Get API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_INBOUND_WEBHOOK_SECRET=

# ============================================================================
# FRONTEND VARIABLES
# ============================================================================

# API URL for frontend
# For Vercel full-stack: use relative path (no CORS needed)
VITE_API_URL=/api

# App Configuration
VITE_APP_NAME=ApprovalFlow
VITE_APP_VERSION=1.0.0

# ============================================================================
# OPTIONAL VARIABLES (Features work without these)
# ============================================================================

# AWS S3 / Cloudflare R2 (for file uploads/attachments)
# If not set, file upload features will be disabled
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=approvalflow-production
AWS_ENDPOINT=

# Firebase Cloud Messaging (for push notifications)
# If not set, push notification features will be disabled
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# CORS Origins (automatically set for Vercel)
# Only needed if deploying to multiple domains
CORS_ORIGIN=*

# ============================================================================
# HOW TO SET ENVIRONMENT VARIABLES IN VERCEL
# ============================================================================
#
# 1. Go to https://vercel.com/dashboard
# 2. Select your project
# 3. Go to Settings → Environment Variables
# 4. For each variable above:
#    - Click "Add New"
#    - Enter Name (e.g., DATABASE_URL)
#    - Enter Value
#    - Select: Production, Preview, Development
#    - Click "Save"
# 5. Redeploy your application
#
# ============================================================================
# QUICKSTART FOR VERCEL DEPLOYMENT
# ============================================================================
#
# Step 1: Add Neon Postgres Integration
#   - Go to Storage tab
#   - Click "Create Database" → "Postgres" (Neon)
#   - This automatically sets DATABASE_URL
#
# Step 2: Generate JWT Secrets
#   Run these commands locally:
#   openssl rand -base64 32  # Copy result for JWT_SECRET
#   openssl rand -base64 32  # Copy result for JWT_REFRESH_SECRET
#
# Step 3: Get SendGrid API Key
#   - Sign up at sendgrid.com
#   - Go to Settings → API Keys
#   - Create new API key with "Full Access"
#   - Set SENDGRID_FROM_EMAIL to your verified sender
#
# Step 4: Set Frontend Variables
#   VITE_API_URL=/api
#   VITE_APP_NAME=ApprovalFlow
#   VITE_APP_VERSION=1.0.0
#
# Step 5: Initialize Database
#   vercel env pull .env.local
#   psql $(grep DATABASE_URL .env.local | cut -d '=' -f2-) -f database/schema.sql
#   psql $(grep DATABASE_URL .env.local | cut -d '=' -f2-) -f database/seed.sql
#
# Step 6: Redeploy
#   git push (auto-deploys) OR
#   Vercel Dashboard → Deployments → Redeploy
#
# ============================================================================
